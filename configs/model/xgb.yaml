# XGBoost params
n_estimators: 5000
learning_rate: 0.01
max_depth: 6
min_child_weight: 5
subsample: 0.8
colsample_bytree: 0.8
reg_alpha: 0.1
reg_lambda: 1.0
early_stopping_rounds: 200

# tune via nested CV (fixes W8/Q1); hardcoded 11 was wrong
scale_pos_weight: 2.5

# XGBoost handles NaN natively (fixes W7: do NOT fillna(-999))
handle_missing: native